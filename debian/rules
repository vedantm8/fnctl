#!/usr/bin/make -f

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_auto_test:
	PYBUILD_SYSTEM=custom PYBUILD_TEST_ARGS="pytest -q" dh_auto_test

execute_before_dh_installdeb:
	chmod +x debian/fnctl.postinst || true
